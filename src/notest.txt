add a placeholder image for the movies that don't have image // search ad astra
when opening the movie, make the loader in the center (currently its on top left)

//  when loading /movie immidiately it gives an error (should be handled)

movie info
- make actor images clickable (redirect to their imdb page)
- make placeholder image if actor doesent have image

- crazy movie clicking gives error
- top rated error in diwalee movie cannot read property key of undefined
-- search for netflix and click netflix acquires error Cannot read property 'name' of undefined
-- image logo should redirect to popular? 

Recommended movies + dark mode